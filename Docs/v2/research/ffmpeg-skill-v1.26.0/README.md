# V2-150 evidence snapshot

This directory is research evidence only. Replex application code, build scripts, tests, and agent tool registration do not load it. No ffmpeg-skill package or runtime dependency was installed in Replex.

`contract.json` and `doctor.json` are the outputs from the pinned upstream source described in `../../ffmpeg-skill-spike.md`. The media fixture and outputs are synthetic. `measurements.json` contains sanitized result summaries and does not contain tool command strings or an argv vector.
